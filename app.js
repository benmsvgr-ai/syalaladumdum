const menu=document.getElementById('menu'), content=document.getElementById('content'), search=document.getElementById('search');let current=0;function drawMenu(){const q=search.value.toLowerCase();menu.innerHTML='';MATERI.forEach((m,i)=>{if((m.title+m.content).toLowerCase().includes(q)){const b=document.createElement('button');b.className=i===current?'active':'';b.innerHTML=`<span>${m.icon}</span>${m.title}`;b.onclick=()=>{current=i;draw()};menu.appendChild(b)}})}function draw(){content.innerHTML=`<section class="page">${MATERI[current].content}</section>`;drawMenu();if(MATERI[current].title==='Bank Soal')startQuiz()}function startQuiz(){const el=document.getElementById('quiz');if(!el)return;el.innerHTML='';QUESTIONS.forEach((x,i)=>{const d=document.createElement('details');let h=`<summary>${i+1}. ${x[0]}</summary>`;x[1].forEach((o,j)=>h+=`<p>${'ABCD'[j]}. ${o}</p>`);h+=`<p><b>Jawaban: ${'ABCD'[x[2]]}</b></p><p>${x[3]}</p>`;d.innerHTML=h;el.appendChild(d)})}search.oninput=drawMenu;drawMenu();draw();